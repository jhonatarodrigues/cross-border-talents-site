import styled from 'styled-components';

import Default from '../../default';

export const LabelDiv = styled.p`
  margin: 0;
  padding: 0.3125rem 0.9375rem;
  border-radius: 0.9375rem;
  font-size: 0.75rem;
  color: ${Default.color.white};
`;
